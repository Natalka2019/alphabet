export const alphabet = "abcdefghijklmnopqrstuvwxyz"
  .toUpperCase()
    .split('')
      // .reduce( (acc: { [key: string]: any[] }, val: string) => {
      //   acc[val] =['-'];
      //   return acc;
      // }, {})